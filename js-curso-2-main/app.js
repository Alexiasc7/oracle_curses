let titulo = document.querySelector('h1');

titulo.innerHTML = 'Hora del desafio';
/*

function clikConsola() {
    console.log("El boton fue clicado");
}

function entradaTexto() {
    let ciudad=prompt('Dime el nombre de una ciudad de Mexico');
    alert(`Estuve en la ciudad ${ciudad} y me acorde de ti`);
}

function yoAmoJS() {
    alert('Yo amo JS');
}

function sumaNumeros() {
    alert('Proporciona dos numeros');
    let numero1=parseInt(prompt('Primer numero'));
    let numero2=parseInt(prompt('Segundo numero'));
    let resultado=numero1+numero2;
    alert(`El resultado de la suma de ambos numeros es ${resultado}`);
}

function mostrarHola() {
    console.log("¡Hola, mundo!");
  }
  
  mostrarHola();

function mostrarNombre(nombre) {
    console.log(`Hola ${nombre}`);
}

  mostrarNombre('Alex');

function calcularDoble(numero) {
  return numero * 2;
}

let resultadoDoble = calcularDoble(5);
console.log(resultadoDoble);

function mostrarPromedio(n1,n2,n3) {
    return (n1+n2+n3)/3;
}

let resultadoPromedio =mostrarPromedio(9,10,10);
console.log(resultadoPromedio);

function encontrarMayor(a, b) {
    return a > b ? a : b;
    //operador ternario ? es if y : es else
  }
  
  let numeroMayor = encontrarMayor(15, 9);
  console.log(numeroMayor);

function multiplicar(numero) {
    return numero*numero;
}
    let numeroMultiplicar= multiplicar(5);
    console.log(numeroMultiplicar);

function claculadorIMC(estaura,peso) {
    return estaura/peso;
}

let calcularMasa=claculadorIMC(157,52);
console.log(calcularMasa);

function calcularFactorial(numero) {
    if (numero === 0 || numero === 1) {
      return 1;
    } else {
      return numero * calcularFactorial(numero - 1); 5 -1,4-1,3-1 hasta llegar a 0
    }
  }
  // Ejemplo de uso
  let numero = 5;
  let resultado = calcularFactorial(numero);
  console.log(`El factorial de ${numero} es ${resultado}`);

function conversorMoneda(pesos) {
    let valorActual= 17.50;
    let conversion= pesos/valorActual;
    return conversion;
}
let pesos=(200);
let total=conversorMoneda(pesos);
console.log(`El total de los ${pesos} pesos MX dados serian ${total} USD`);
 
function  clacularAreaPerimetro(altura,base) {
    let area =altura*base;
    let perimetro = 2*(altura + base);
    console.log(`La altura de los valores dados es ${area}`);
    console.log(`El perimetro de la base dada es de ${perimetro}`);
}

let base=5;
let altura=10;
clacularAreaPerimetro(altura,base);

function calcularAreaYPerimetroCircular(radio) {
    var pi = 3.14;
    var area = pi * radio * radio;
    var perimetro = 2 * pi * radio;
    console.log("Área: " + area);
    console.log("Perímetro: " + perimetro);
  }
  // Ejemplo de uso
  let radio = 4; // en metros
  calcularAreaYPerimetroCircular(radio);
  
 function tablaMultiplicar(numero) {
    for (var i = 1; i <= 10; i++) {
        var resultado = numero * i;
        console.log(numero + " x " + i + " = " + resultado);
      }
 }
let numero=5;
tablaMultiplicar(numero);*/